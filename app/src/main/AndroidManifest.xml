<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lesson">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Podsumowanie"></activity>
        <activity android:name=".Publikacja3" />
        <activity android:name=".Publikacja2" />
        <activity android:name=".Publikacja1" />
        <activity android:name=".Test3" />
        <activity android:name=".Test2" />
        <activity android:name=".Test1" />
        <activity android:name=".Lesson3" />
        <activity android:name=".Lesson2" />
        <activity android:name=".Lesson1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>